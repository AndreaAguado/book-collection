import { render, screen } from '@testing-library/react';
import BookDetail from '../components/BookDetail';

test('ISBN is `not available` when falsy', () => {
    // let bookToRender = {
    //     title: 'Alice in wonderland',
    //     first_name: 'Lewis',
    //     last_name: 'Carroll',
    //     isbn: '',
    // }

    // render(<BookDetail bookToRender={bookToRender}></BookDetail>)

    // const isbn = screen.getByText("ISBN:");
    // expect(isbn).toBe('Not available');
})
